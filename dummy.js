/*
$(selector.form.id).form('set values', response.data);
// Tem que ser revisto, pois foge da modularidade
// Muda a tab
$.tab('change tab', 'item');    
//Atualiza a call/API do formulário pra update
_form.formUpdate();
*/
/* Menu Module ...
$('.menu .item').attr('class', 'item');
$('.menu #items').attr('class', 'active item');
*/